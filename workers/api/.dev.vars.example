# Local overrides for the API worker. Copy to .dev.vars (gitignored);
# `bun run setup` does this for you.

# Signs sessions and tokens. Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=replace-me

# Where the web app runs locally
WEB_URL=http://localhost:3000
